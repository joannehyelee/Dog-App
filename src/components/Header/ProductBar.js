import React from "react";

const ProductBar = () => (
    <div className="gds-page-header__product-bar">
        <h6 className="gds-page-header__product-name">Dog App</h6>
        <img className="gds-page-header__logo gds-page-header__logo--expanded" />
    </div>
);

export default ProductBar;